---
import { IMAGES_PATH } from '@/services/consts/const-imagesPath'
const { credits } = Astro.props
---

<article class="relative flex h-full w-full flex-col items-center gap-22 p-4">
  <h4 class="animate-fade-in-up mt-20 text-center text-4xl font-bold text-white">Reparto</h4>
  <div class="flex flex-wrap items-start justify-center gap-12">
    {
      credits.cast.map((actor) => {
        if (actor.known_for_department === 'Acting') {
          return (
            <div class="flex h-auto w-38 flex-col items-center justify-center gap-2">
              <img
                src={`${IMAGES_PATH}${actor.profile_path}`}
                alt={actor.name}
                class="h-[90%] justify-self-start rounded-lg object-center shadow-md transition-transform duration-300 ease-in-out hover:scale-105"
              />

              <span class="text-center text-sm font-bold text-white">
                {actor.name} ({actor.character})
              </span>
            </div>
          )
        }
      })
    }
  </div>
</article>
